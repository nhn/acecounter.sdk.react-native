export enum SDKMode {
  development,
  production,
}

export enum NetworkMode {
  COMPANY_dev,
  HOME_dev,
  Pro,
}

export enum NetworkRequestType {
  LOG,
  POLICY,
}

export enum NetworkRequestType2 {
  COMPANY_LOCAL,
  HOME_LOCAL_LOG,
  HOME_LOCAL_POLICY,
  LOG,
  POLICY,
}
